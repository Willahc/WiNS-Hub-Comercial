import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';
const src=fs.readFileSync(path.resolve(__dirname,'../pages/AgroTecnicaApproved.tsx'),'utf8');
describe('Canal Técnico',()=>{it('possui KPIs, filtros, paginação e ficha',()=>{for(const value of ['Total no canal','Profissionais nominais','Estabelecimentos','Veterinários','Zootecnistas','Reprodução/manejo','CRMV informado','Com telefone/e-mail','Busca','Município','Anterior','Próxima','Ver ficha'])expect(src).toContain(value)});it('separa badges e não valida CRMV oficialmente',()=>{expect(src).toContain('Profissional nominal');expect(src).toContain('Estabelecimento');expect(src).toContain('Provável por CNAE');expect(src).toContain('situação não validada');expect(src).toContain('CREA');expect(src).toContain('ABCZ');expect(src).toContain("typeof i.score_canal==='number'")})});
