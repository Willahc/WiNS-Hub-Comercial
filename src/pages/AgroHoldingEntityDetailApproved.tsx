import React,{useCallback,useEffect,useState}from'react';import{Link,useParams}from'react-router-dom';import AgroPageShell from'../components/AgroPageShell';import{httpClient}from'../services/http/client';import{AGRO_API}from'./agroApiEndpoints';export default function AgroHoldingEntityDetailApproved(){const{id=''}=useParams(),[data,setData]=useState<any>(null),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{setLoading(true);setError(null);try{setData((await httpClient.get(AGRO_API.holdingEntity(id))).data)}catch{setError('Não foi possível carregar a empresa.')}finally{setLoading(false)}},[id]);useEffect(()=>{load()},[load]);const x=data?.entity;return <AgroPageShell title={x?.razao_social||'Detalhe da empresa'} subtitle="Cadastro e evidências no recorte Holdings Agro" backTo="/agro/holdings" loading={loading} error={error} onRetry={load} empty={!loading&&!error&&!x} emptyMessage="Empresa não encontrada.">{x&&<><section className="holding-detail"><div><small>CNPJ</small><strong>{x.cnpj}</strong></div><div><small>Classificação</small><strong>{x.tipo_entidade}</strong></div><div><small>CNAE</small><strong>{x.cnae_principal||'Não disponível'}</strong></div><div><small>Município / UF</small><strong>{x.municipio||'Não disponível'} / {x.uf||'—'}</strong></div></section><section><h2>Motivo Agro</h2><p>{x.motivo_inclusao}: {x.motivo_descricao}</p><small>Fonte: {x.motivo_fonte}</small></section><section><h2>Pessoa vinculada selecionada</h2>{x.pessoa_selecionada_id?<Link to={`/agro/leads/${x.pessoa_selecionada_id}`}>{x.pessoa_selecionada_nome} — {x.pessoa_selecionada_tipo_vinculo}</Link>:<p>Não disponível.</p>}</section><section><h2>Evidências</h2><p>Societária: {x.evidencia_societaria}</p><p>Grupo: {x.evidencia_grupo}</p><p>Agro: {x.evidencia_agro}</p><p>Ativo rural: {x.evidencia_ativo_rural}</p></section><section><h2>Grupo documental</h2><p>{data.message_group||'Nenhum grupo econômico documentalmente comprovado foi identificado.'}</p></section><section><h2>Propriedades comprovadas</h2><p>{data.properties===null?'Nenhuma propriedade documentalmente vinculada foi disponibilizada.':'Propriedades documentais disponíveis.'}</p></section>{data.company_360_url&&<Link to={data.company_360_url}>Abrir Empresa 360°</Link>}<section><h2>Fontes e limitações</h2>{(data.sources||[]).map((v:string)=><p key={v}>{v}</p>)}{(data.limitations||[]).map((v:string)=><p key={v}>{v}</p>)}</section></>}</AgroPageShell>}
