services:
  engenharia-api:
    build:
      context: .
    image: wins-hub-engenharia:r2
    restart: unless-stopped
    mem_limit: 256m
    env_file: engenharia.env
    environment:
      TZ: America/Sao_Paulo
      APPLICATION_ROOT: /engenharia
    ports:
      - "127.0.0.1:18081:8000"
